# Evan Meagher's Emacs configuration

An Emacs configuration tailored to [Emacs for macOS](https://emacsformacosx.com/).

## Installation

```
git clone git://github.com/evnm/emacs.git ~/.emacs.d
```
